# سياق المشروع — مؤسسة لون للمقاولات العامة

> هذا الملف يُقرأ تلقائياً في بداية كل جلسة. يشرح **القرارات وأسبابها**
> والمصائد المكتشفة — لا تاريخ التعديلات. اقرأه قبل أي تغيير.

---

## ما هو المشروع

موقع صفحة واحدة لمؤسسة مقاولات سعودية متخصصة في **الدهانات**. الهدف: بناء الثقة وتحويل الزائر إلى اتصال/واتساب.

**عربي (افتراضي، RTL) + إنجليزي (LTR)** بتبديل حي.

---

## ⛔ لا تفعل هذا

| | لماذا |
|---|---|
| **لا تحوّله إلى React/Next/Tailwind** | HTML+CSS+JS نقي، بلا build وبلا npm. صفر اعتماديات = رفع على أي استضافة بالسحب والإفلات. عُرض التحويل ورُفض عمداً. |
| **لا تضف عنصر `<video>` للهيرو** | جُرِّب وفشل — انظر «مشهد الهيرو» أدناه. |
| **الأحمر هو لون الهوية الآن** | كان ذهبياً ومحجوزاً الأحمر للشعار؛ المالك قرّر الأحمر (سبتمبر ٢٠٢٦). أحمر الشعار `#d80000` **يرسب كنصّ على الداكن (3.49)** — لذلك نصّ الداكن `#ff6b60` والفاتح `#c40000`، والتعبئة وحدها بلون الشعار مع نصّ أبيض. |
| **لا تخمّن قيم التباين أو الحجاب** | كلها مقاسة. انظر «القياس» أدناه. |
| **لا تعكس ترتيب السكربتات** | `coverflow.js` **قبل** `motion.js`. |
| **لا تستعمل مِزلاج `rAF`** | يتعطّل للأبد في التبويبات الخلفية. |

---

## بنية الملفات

```
index.html                 البنية والمحتوى (اللغتان عبر data-i18n)
assets/css/styles.css      نظام التصميم كاملاً
assets/js/i18n.js          ⚠️ بيانات المؤسسة + نصوص اللغتين + الثيم
assets/js/coverflow.js     معرض الأعمال ثلاثي الأبعاد
assets/js/hero-scene.js    مشهد الهيرو المرتبط بالتمرير
assets/js/motion.js        باقي التفاعل + GSAP
assets/frames/f001..f060   إطارات مشهد الهيرو (٣ م.ب)
assets/img/logo.svg        الشعار (إعادة رسم تقريبية)
assets/video/              غير مستخدم — مصدر فقط
README.md                  دليل التعديل للمالك
```

---

## نظام الألوان والثيم

**الهوية:** أحمر الشعار على محايد دافئ (كانت ذهبية حتى سبتمبر ٢٠٢٦؛ الفقرة التالية تشرح أصل المحايد الدافئ).

مشتقة من قاعدة `ui-ux-pro-max` (صفوف Construction/Architecture و Architecture/Interior و Home Decoration). بنيتها الثلاث واحدة: محايد يحمل الواجهة + لمسة دافئة واحدة. والذهبي **جار للأحمر** على عجلة الألوان فينسجم مع الشعار — بخلاف البترولي (مُكمّل) الذي جُرّب أولاً وكان يزاحمه.

### الرموز — كلها في أعلى `styles.css`

| الرمز | الاستخدام |
|---|---|
| `--brand` | **النص والخطوط والأيقونات وحلقة التركيز** |
| `--brand-solid` | **تعبئة** الأزرار فقط |
| `--on-brand` | نص الزر — أبيض في الفاتح، **داكن** في الداكن |
| `--logo` | الشعار لا غير |
| `--bg` / `--surface` / `--surface-2` | أسطح متدرّجة |
| `--fg` / `--muted` | نص أساسي / ثانوي |
| `--fg-rgb` / `--bg-rgb` | **تُولّد كل الشعيرات والتظليل** فتنقلب مع الثيم بلا قاعدة مكرّرة |
| `--on-scrim` | النص فوق الصور (ثابت فاتح — الحجاب ثابت داكن) |
| `--scrim-rgb` | حجاب الصور (ثابت داكن في الثيمين) |

### 🪤 مصائد مكتشفة

1. **`--brand` ≠ `--brand-solid`.** استعمال لون التعبئة كنص على الداكن يعطي **2.82:1 ويرسب**. حدث فعلاً في `.stat__num` وحلقة التركيز.
2. **لا تثبّت لون نص الزر.** الذهبي الفاتح في الوضع الداكن يحتاج نصاً **داكناً** لا أبيض (الأبيض 4.11 يرسب).
3. **`--fg` لون نص لا سطح.** استبدال شامل قديم جعل `.stat` تُطلى بلون النص. تحقّق دائماً: `grep 'background.*var(--fg)'`.
4. **الأقسام `--base` / `--alt` لا «داكن/فاتح».** الثبات على داكن/فاتح يتعارض جذرياً مع الثيم.
5. **الهيرو والشريط العلوي فوق صورة داكنة دائماً** — نصوصهما لا تتبع الثيم. الشريط يستعمل `--nav-fg` الذي يتبدّل بين `--on-scrim` (شفاف) و`--fg` (بعد الظهور).

### زر الثيم

في شريط التنقل. يُحفظ في `localStorage`، وبلا اختيار يتبع النظام. **يُطبَّق في `<head>` قبل أول رسم** — تأجيله يعني وميض الثيم الخطأ.

---

## مشهد الهيرو — تسلسل صور لا فيديو

`<canvas>` يرسم ٦٠ صورة حسب موضع التمرير. الهيرو `260vh` وداخله مسرح `sticky`.

**لماذا ليس فيديو؟** جُرّب وفشل لثلاثة أسباب متتالية، وكلها خارج سيطرة الموقع:
1. الملف الأصلي فيه **إطار مفتاحي واحد لـ٢٤٠ إطاراً** — كل بحث يفكّ الترميز من البداية فيتجمّد.
2. حتى بعد إعادة الترميز (٤٠ إطاراً مفتاحياً)، محرّك الوسائط يؤجّل التحميل في التبويبات الخلفية.
3. الصور تمرّ بمسار مختلف ولا تفشل، والانتقال لأي لحظة فوري.

**لتغيير المشهد:**
```bash
ffmpeg -i new.mp4 -vf "fps=6,scale=1280:-2" -q:v 8 assets/frames/f%03d.jpg
```
ثم عدّل `FRAME_COUNT` في `hero-scene.js` و`.hero { height }` بمعدل `16vh` لكل ثانية.

### الحجاب — بركة مركّزة لا تدرّج

`radial-gradient` ممتدّ رأسياً (70%) خلف النص. **ليس تدرّجاً خطياً** لأن كتلة النص عريضة (٥٨٪ من الشاشة) فكان التدرّج الخطي مضطراً للبقاء ثقيلاً عبر معظم اللقطة.

> **البيضاوي ممتدّ رأسياً عمداً** حتى تخرج حوافه عن الشاشة. النسخة الأضيق كانت أخف (٤١٪ بدل ٥٠٪) لكنها **رسمت قوساً مرئياً في السماء** — السماء سطح أملس يكشف أي حدّ.

للجوال قاعدة منفصلة (النص يملأ العرض هناك).

---

## 📏 القياس — كل الأرقام مقاسة لا مقدّرة

**لا تغيّر لوناً أو حجاباً بالذوق.** الطريقة المستعملة:

```bash
# استخراج بكسلات نطاق النص من كل الإطارات
ffmpeg -i assets/frames/fNNN.jpg -vf "crop=iw*0.65:ih*0.65:iw*0.35:ih*0.18,scale=24:14" \
  -f rawvideo -pix_fmt rgb24 out.rgb
```
ثم في node: تركيب الحجاب على كل بكسل وحساب WCAG، مع **نموذج واقعي لتوزيع النص**:

| النطاق | الموضع | المطلوب |
|---|---|---|
| عنوان القسم (13px ذهبي) | اليمين 0–30% | 4.5 |
| العنوان الضخم | اليمين 0–65% | 3.0 |
| المتن | اليمين 0–65% | 4.5 |

**العنصر الحاكم هو عنوان القسم الصغير، لا المتن.**

النتائج الحالية: الحجاب 5.39 / 6.00 / 9.13 · الألوان ١٨ زوجاً × ثيمين بصفر رسوب.

> لو غيّرت إطارات المشهد لصور أفتح، **أعد القياس**.

---

## سلوكيات مهمة

**الشريط العلوي** مخفي ما دام المشهد يُعرَض (`hero.getBoundingClientRect().bottom <= 80`)، ثم ينزلق ظاهراً. يُخفى بـ `transform`+`opacity` لا `display:none` ليبقى في شجرة إمكانية الوصول، و`.nav:focus-within` يُظهره لمستخدم لوحة المفاتيح.

**المعرض (coverflow)** منقول من مكوّن React إلى DOM خالص — المعادلات مطابقة رقمياً (تحقّق: `tz=-291.905`, `rotateY(-64.8678)` عند البطاقة الثانية). النقر على بطاقة جانبية يوسّطها، والمركزية تفتح العارض، والسحب لا يُحسب نقرة. **هذا يتطلب تحميل `coverflow.js` قبل `motion.js`.**

**قبل/بعد** جدار مرسوم بـCSS لا صورة — حتى لا يدّعي الموقع مشروعاً لم يُنفَّذ. المقبض `<input type="range">` شفاف فيعمل بالكيبورد وقارئ الشاشة مجاناً.

**`prefers-reduced-motion`**: يُلغي الحركات الزخرفية، لكن **لا يُلغي مشهد الهيرو** — لأنه لا يتحرك من تلقاء نفسه، يتقدّم بمقدار ما يمرّر الزائر.

---

## 🔧 بيئة التطوير

| | الحالة |
|---|---|
| Node.js | ✅ v24 |
| ffmpeg / ffprobe | ✅ (winget) |
| **Python** | ❌ **غير مثبّت** — مهارة `ui-ux-pro-max` تمنع تثبيته. اقرأ ملفات `.claude/skills/ui-ux-pro-max/data/*.csv` مباشرة بـnode. |

### ⚠️ الفحص البصري

**لوحة المعاينة المدمجة غير موثوقة:** تُجمّد الملفات، تعرض لقطات قديمة، تفرض `prefers-reduced-motion`، وتعرض الصفحة أحياناً بلا أنماط. **أضاعت وقتاً كثيراً وأنتجت استنتاجات خاطئة.**

استعمل بدلها خادماً محلياً + كروم الحقيقي:
```bash
npx --yes serve "C:\Users\amin1\OneDrive\سطح المكتب\Website" -l 8080
```

**وفي كروم عبر الإضافة:** كل استدعاء جافاسكربت يجعل التبويب `hidden` — فـ`requestAnimationFrame` يتوقف، والانتقالات تتجمّد، و`scrollTo` **لا يُطلق أحداث تمرير**. أي قياس يعتمد على ذلك **باطل**. استعمل أداة `computer` للتمرير الحقيقي، واقرأ الحالة بالقياس المباشر لا باللقطات.

> جهاز المستخدم عليه **«تقليل الحركة» مفعّل** في ويندوز (Settings ← Accessibility ← Visual effects ← Animation effects).

---

## 📌 متبقٍ للمالك (قيم مؤقتة)

| البند | المكان |
|---|---|
| أرقام التواصل والسجل التجاري | كتلة `BIZ` أعلى `i18n.js` — **المكان الوحيد** |
| الشعار الأصلي (SVG) | `assets/img/logo.svg` + ثلاثة مواضع في `index.html` |
| صور المشاريع | قسم `#work` — الحالية من Unsplash، **مُتحقَّق منها بصرياً** |
| أرقام «من نحن» | `data-count` في `index.html` |
| صورتا قبل/بعد حقيقيتان | قسم `#reveal` |

**التسميات تصف نوع العمل لا مشروعاً بعينه** — عمداً، حتى لا يدّعي الموقع ما لم يُنفَّذ. غيّرها لأسماء المشاريع عند رفع الصور الحقيقية.

النموذج بلا خادم — يفتح واتساب برسالة مُعبّأة.
